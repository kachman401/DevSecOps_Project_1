version: '3.8'

services:
  app:
    build:
      context: ./docker
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ENV=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge