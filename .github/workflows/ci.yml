name: CI Pipeline

on:
  push:
    branches:
      - main
      - ci/setup
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Node.js setup is not needed since this project isn't a Node app
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 18

      # Commented out npm commands since there's no package.json file
      # - name: Install dependencies
      #   run: npm install

      # - name: Run tests
      #   run: npm test
